# DeepSeek-OCR Requirements
# Exact versions from working Colab notebook

# Core ML libraries (MUST install torch first with CUDA support)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Transformers ecosystem
transformers==4.46.3
tokenizers==0.20.3

# Model dependencies
einops
addict
easydict

# Image processing
Pillow
pdf2image

# Optimization
bitsandbytes
accelerate

# Document processing (keep minimal; avoid disallowed OCR libs)
img2pdf

# API
google-generativeai

# RunPod serverless
runpod
